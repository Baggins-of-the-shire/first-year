# First Year

A single-page gallery of nature photographs — mushrooms and wildlife — built as plain, hand-written HTML and CSS: no JavaScript, no build step, no external dependencies, with `photos.json` keeping a record of every photo's metadata. To view it, open `index.html` directly in any browser (it works straight from a local folder), or serve the directory with any static file server.
